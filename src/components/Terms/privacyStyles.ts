import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles(theme => ({
  root: {
    width: '100%'
  },
  section: {
    margin: '20px 0'
  },
  subsection: {
    margin: '20px 0 0 5px'
  }
}));

export default useStyles;
